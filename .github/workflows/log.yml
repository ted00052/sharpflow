name: edge-logger
on:
  schedule: [{cron: "*/30 22-23 * * *"}, {cron: "*/30 0-5 * * *"}, {cron: "15 16-21 * * *"}, {cron: "15 6-15/3 * * *"}]  # time-targeted: dense in US game windows
  workflow_dispatch: {}     # manual run button

permissions:
  contents: write

concurrency:
  group: edge-logger
  cancel-in-progress: false

jobs:
  log:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Run logger
        run: python edge_logger.py --min-edge 2

      - name: Commit log + page
        run: |
          git config user.name "sharpflow-edge-logger"
          git config user.email "actions@users.noreply.github.com"
          git add data docs
          if git diff --cached --quiet; then
            echo "No changes this run."
          else
            git commit -m "log: $(date -u +'%Y-%m-%d %H:%M') update"
            git push
          fi
